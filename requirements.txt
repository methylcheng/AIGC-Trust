# ============================================
# AIGC-Trust 项目依赖清单
# Python 版本要求: 3.13
# ============================================

# Web Framework
fastapi>=0.115.0
uvicorn>=0.32.0

# Database
sqlalchemy>=2.0.36
pymysql>=1.1.1

# Message Queue
pika>=1.3.2

# Cryptography (国密算法)
gmssl==3.2.2
cryptography>=44.0.0
pycryptodome>=3.23.0
pycryptodomex>=3.23.0

# Image Processing
opencv-python>=4.11.0
numpy>=1.23.0
Pillow>=10.4.0

# Deep Learning (PyTorch - 支持 Python 3.13 + GPU)
# 注意: PyTorch GPU 版本需要通过以下命令单独安装:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.6.0
torchvision>=0.17.0
torchaudio>=2.6.0

# Utilities
python-multipart>=0.0.6
requests>=2.32.0
tqdm>=4.67.0

# Document Processing (Word & PDF)
python-docx>=1.1.0
PyPDF2>=3.0.0

# Testing (可选)
pytest>=9.0.0

# Other Dependencies (自动安装,无需手动指定)
# aiohttp, anyio, click, colorama, cffi, greenlet, h11, idna
# jinja2, joblib, networkx, packaging, pydantic, starlette
# typing-extensions, urllib3, yarl 等
